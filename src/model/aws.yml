service: xxx

provider:
  name: aws
  runtime: nodejs12.x
  lambdaHashingVersion: 20201221
  region: xxx
  stage: xxx

plugins:
  - serverless-apigw-binary

custom:
  apigwBinary:
    types:
      - '*/*'
  customDomain:
    domainName: xxx
    certificateName: xxx
    basePath: ''
    stage: xxx

functions:
  aws:
    handler: index.aws
    events:
      - http: ANY /
      - http: 'ANY /{proxy+}'
